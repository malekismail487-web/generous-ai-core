import { OMEGA_R1_REPLICATION_SPEC_001,R1_REPLICATION_REQUIREMENTS,assessR1ReplicationManifest,validateR1ReplicationSpecification,type R1ReplicationManifest } from "../src/lib/codelab/assurance/r1ReplicationSpec";
let passed=0;let failed=0;const failures:string[]=[];function assert(v:unknown,l:string):void{if(v)passed+=1;else{failed+=1;failures.push(l);console.error(`  x ${l}`)}}
assert(validateR1ReplicationSpecification(OMEGA_R1_REPLICATION_SPEC_001).length===0,"R1 replication specification contains every reconstructibility contract");assert(OMEGA_R1_REPLICATION_SPEC_001.executionState==="NOT_EXECUTED"&&OMEGA_R1_REPLICATION_SPEC_001.maturity==="SPECIFIED","replication specification does not claim execution");assert(OMEGA_R1_REPLICATION_SPEC_001.environmentPolicy.deno==="BLOCKED_ENVIRONMENT_NOT_REQUIRED"&&OMEGA_R1_REPLICATION_SPEC_001.environmentPolicy.liveNetwork==="NOT_AUTHORIZED_NOT_EXECUTED","Deno and network remain honestly blocked or unauthorized");assert(!OMEGA_R1_REPLICATION_SPEC_001.grantsAuthority&&OMEGA_R1_REPLICATION_SPEC_001.authorityCeiling==="READ_REPOSITORY","replication spec preserves R1 authority");
const manifest:R1ReplicationManifest={schemaVersion:1,replicationId:"REPLICA-1",candidateCommit:OMEGA_R1_REPLICATION_SPEC_001.candidateCommit,suiteManifestDigest:"a".repeat(64),environmentIdentity:"clean-windows-worker-1",installedTools:{node:"24.19.0",typescript:"5.8.3"},executedRequirements:R1_REPLICATION_REQUIREMENTS,blockedRequirements:[],skippedRequirements:[],authority:"READ_REPOSITORY",evidenceRefs:["evidence://replication"]};
assert(assessR1ReplicationManifest(manifest).decision==="COMPLETE_RECORD","complete future replication manifest is reconstructible");assert(!assessR1ReplicationManifest(manifest).replicationAccepted,"record completeness alone does not accept replication");assert(assessR1ReplicationManifest({...manifest,candidateCommit:"b".repeat(40)}).decision==="WRONG_CANDIDATE","wrong replication candidate is explicit");assert(assessR1ReplicationManifest({...manifest,evidenceRefs:[]}).decision==="INCOMPLETE_RECORD","replication without evidence provenance is incomplete");
console.log(`Omega R1 replication specification tests - passed: ${passed}, failed: ${failed}`);if(failed){for(const f of failures)console.error(`  - ${f}`);process.exit(1)}
