project_id = "lumina-local"

[functions.lumina-api]
verify_jwt = false

[functions.ale-api]
# Intentionally public only so the deployed tombstone can return an explicit
# 410 response. The handler contains no authentication or downstream path.
verify_jwt = false
